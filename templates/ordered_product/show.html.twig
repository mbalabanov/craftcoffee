{% extends 'base.html.twig' %}

{% block title %}OrderedProduct{% endblock %}

{% block body %}
    {# <h1>OrderedProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>OrderedProductId</th>
                <td>{{ ordered_product.orderedProductId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordered_product_index') }}">back to list</a>

    <a href="{{ path('ordered_product_edit', {'orderedProductId': ordered_product.orderedProductId}) }}">edit</a> #}

    {{ include('ordered_product/_delete_form.html.twig') }}
{% endblock %}
