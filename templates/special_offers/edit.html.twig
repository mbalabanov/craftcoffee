{% extends 'base.html.twig' %}

{% block title %}Edit SpecialOffers{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row my-4">
        <div class="col-md-8 offset-2">
            <h3>Edit "{{ special_offer.name }}"</h3>
            <a href="{{ path('special_offers_index') }}" class="btn btn-warning">Back to Special Offers</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8 offset-2">
            {{ include('special_offers/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('special_offers/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
